# Docker Environment Configuration for Studio Revenue Manager
# Copy this file to .env.docker and update with your values

# ===============================================
# Database Configuration
# ===============================================
POSTGRES_DB=studio_revenue_manager
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_PORT=5432

# ===============================================
# Backend Server Configuration
# ===============================================
PORT=3000
BACKEND_PORT=3000
NODE_ENV=production

# ===============================================
# Authentication
# ===============================================
# IMPORTANT: Generate a strong random secret for production
# Example: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE-THIS-TO-A-RANDOM-SECRET-IN-PRODUCTION

# ===============================================
# Google Calendar API (Optional)
# ===============================================
GOOGLE_CALENDAR_API_KEY=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_SERVICE_ACCOUNT_PRIVATE_KEY=

# ===============================================
# Email Configuration (Optional)
# ===============================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# ===============================================
# Development Notes
# ===============================================
# 1. Copy this file to .env.docker: cp .env.docker.example .env.docker
# 2. Update POSTGRES_PASSWORD and JWT_SECRET with secure values
# 3. Configure optional services (Google Calendar, SMTP) as needed
# 4. Run: docker-compose --env-file .env.docker up --build -d
